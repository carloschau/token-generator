apiVersion: v1
kind: Service
metadata:
  name: token-generator-service
spec:
  type: LoadBalancer
  selector:
    app: token-generator
  ports:
    - name: https
      protocol: TCP
      port: 443
      targetPort: 8080
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080